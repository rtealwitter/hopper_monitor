[project]
name = "hopper-monitor"
version = "0.1.0"
description = "Public GPU/CPU/queue utilization tracker for hopper.cluster"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.1",
]
